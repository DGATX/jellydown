# ===========================================
# JellyDown Configuration
# ===========================================

# Server Configuration
PORT=6942
NODE_ENV=production

# Session secret - IMPORTANT: Generate a random string for production!
# Example: openssl rand -base64 32
SESSION_SECRET=change-this-to-a-random-secret

# ===========================================
# Directory Paths
# ===========================================

# Completed downloads directory
DOWNLOADS_DIR=./downloads

# Temporary files and segment cache
TEMP_DIR=./temp

# Settings and data persistence
DATA_DIR=./data

# ===========================================
# Download Settings
# ===========================================

# Number of segments downloaded in parallel per file (1-10)
MAX_CONCURRENT_SEGMENTS=3

# Retry attempts per segment on failure
MAX_RETRIES=5

# Timeout per segment in milliseconds (2 minutes default)
# Increase if you have slow transcoding or network
SEGMENT_TIMEOUT_MS=120000

# ===========================================
# Session Settings
# ===========================================

# Session duration in milliseconds (24 hours default)
SESSION_MAX_AGE_MS=86400000

# ===========================================
# Reverse Proxy Settings
# ===========================================

# Trust reverse proxy headers (X-Forwarded-For, X-Forwarded-Proto)
# Options: true, false, or number of proxy hops
# Set to 'true' when behind nginx, Traefik, Caddy, etc.
TRUST_PROXY=true
